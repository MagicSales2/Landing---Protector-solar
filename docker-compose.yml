version: '3.8'

# ==========================================================
# 🐳 Docker Compose - High Conversion Anthelios Landing Page
# ==========================================================

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anthelios_landing_web
    ports:
      - "3001:80"
    restart: always
    environment:
      - NODE_ENV=production
